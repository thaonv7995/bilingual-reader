#!/bin/sh
for arg in "$@"; do
  case "$arg" in
    http*)
      if [ -n "$AGY_OAUTH_URL_FILE" ]; then
        printf '%s\n' "$arg" > "$AGY_OAUTH_URL_FILE"
      fi
      exit 1
      ;;
  esac
done
exit 1
