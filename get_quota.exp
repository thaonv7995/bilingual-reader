#!/usr/bin/expect -f
set stty_init "cols 1000"
spawn agy

expect ">" {
    send "models\r"
    # Wait for the output of models to finish, maybe look for "esc to cancel" or something
    expect "esc to cancel" {
        send "\x1b"
        expect ">"
        send "exit\r"
    }
}
expect eof
