#!/usr/bin/expect -f
spawn agy
expect "Select login method:"
send "1\r"
expect "https://"
# We can't capture the rest of the line easily in expect if we don't write it to a file
# So let's just let it log everything to stdout!
set timeout 5
expect eof
